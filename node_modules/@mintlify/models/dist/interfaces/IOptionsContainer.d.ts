/**
 * Container to store, update and retrieve options.
 * @template T - Type of the options.
 */
export interface IOptionsContainer<T> {
    /**
     * Gets the current options stored in the container.
     **/
    readonly opts: Readonly<T>;
}
